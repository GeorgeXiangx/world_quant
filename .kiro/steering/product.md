# Product

This is a WorldQuant Brain alpha research automation tool. It authenticates with the Brain API, fetches data fields from a specified dataset, generates alpha expressions by combining those fields with group and time-series operators, and submits them as simulations to the Brain platform.

Target universe: US equities (TOP3000), delay 1, focused on systematic alpha discovery via combinatorial expression generation.
